"""
Account models.

This module will contain Account model definitions once migrated from
the legacy crm_accounts_models.py file.

For now, this serves as a placeholder showing the future structure.
"""

# Future: Import and define Account model here
# from django.db import models
# from crm_package.core.tenancy.mixins import TenantOwnedModel
#
# class Account(TenantOwnedModel):
#     """Account/Company entity"""
#     name = models.CharField(max_length=255)
#     # ... other fields
